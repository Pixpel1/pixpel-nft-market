import Layout from "@dex/components/templates/Layout";
import Swap from "@dex/components/templates/limit/Swap";
import React from "react";

const liquidity = () => {
    return (
        <Layout>
            <Swap />
        </Layout>
    );
};

export default liquidity;
